# Task

Use the supplied Reuse Before Build skill. Resume the repository task using
the original user request in `context/original-request.md` and the saved
continuation note in `context/handoff.md`.

Work only in this fixture. Do not use the network or install packages.
