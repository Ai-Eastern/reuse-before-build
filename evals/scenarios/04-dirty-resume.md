# Task

Use the supplied Reuse Before Build skill. Continue the task described in
`context/original-request.md`, using `context/handoff.md` and its referenced
verification record as continuation context.

Do not use the network, install packages, commit, or push.
